#!/bin/bash
app=$1;
cd $app || exit 1

if [ -e Gemfile ]; then
    exec sh -c "exec bundle exec puma -C ../../shared/config/puma.rb --daemon"
else
    exec sh -c "exec puma -C ../../shared/config/puma.rb --daemon"
fi
